Change_grade.py

This module takes the parameters of a string and an integer. The module
the searches for the string within the specified file and replaces the grade
on that line with the parameter given. It will then output the file including
the changed lines.
